---
type Props = {
  sectionClass: string
  containerClass?: string
  id: string
}

const { sectionClass, containerClass, id }: Props = Astro.props
---

<section id={id} class={`${sectionClass}  padding-block-10`}>
  <div class={`container ${containerClass}`}>
    <slot />
  </div>
</section>
